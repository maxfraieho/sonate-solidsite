# Claude CLI Prompts — Localization Fix v4.0

## 🔴🔴🔴 НАЙКРИТИЧНІША ПРОБЛЕМА: Ключі i18n не збігаються!

**HTML ключі НЕ відповідають JSON структурі:**
- `data-i18n="hero.supportCta"` → JSON має `hero.cta_support`
- `data-i18n="manifesto.v.title"` → JSON має `manifesto.values.v_title`
- `data-i18n="quote.arsen"` → JSON має `manifesto.quote`

**Результат**: i18n-engine.js завантажує JSON, але НЕ МОЖЕ знайти переклади

## Швидкий старт

```bash
cd ~/violin.pp.ua

# 1. Виконати TASK_06 (виправлення ключів)
chmod +x fix-i18n-keys.sh
./fix-i18n-keys.sh

# 2. Перевірити
grep -c 'manifesto.values.v_title' index.html  # має бути > 0

# 3. Комміт
git add -A && git commit -m "fix(i18n): align keys" && git push
```

## Структура промптів (порядок виконання!)

| # | Файл | Опис | Пріоритет |
|---|------|------|-----------|
| **6** | `TASK_06_FIX_I18N_KEYS.md` | **Виправити невідповідність ключів** | 🔴🔴🔴 ЗАРАЗ! |
| 0 | `TASK_05_FIX_ASSET_PATHS.md` | Виправити шляхи CSS/JS | ✅ Зроблено |
| 1 | `TASK_01_I18N_ENGINE.md` | Створити i18n-engine.js | ✅ Зроблено |
| 2 | `TASK_02_CONNECT_I18N.md` | Підключити до HTML | ✅ Зроблено |
| 3 | `TASK_03_LANG_SWITCHER.md` | Перемикач на всі сторінки | 🟡 Частково |
| 4 | `TASK_04_NAV_LINKS.md` | Навігація з мовою | 🟡 Частково |

## Діагностика проблеми

```bash
# Перевірити чи старі ключі ще є в HTML:
grep -r 'hero\.supportCta' *.html */*.html  # НЕ має бути результатів
grep -r 'manifesto\.v\.title' *.html */*.html  # НЕ має бути результатів

# Перевірити чи нові ключі є:
grep -r 'hero\.cta_support' *.html */*.html  # МАЄ бути результати
grep -r 'manifesto\.values\.v_title' *.html */*.html  # МАЄ бути результати
```

## Очікуваний результат після TASK_06

- ✅ `/de/index.html` → **німецький текст** (не французький!)
- ✅ `/uk/index.html` → **український текст**
- ✅ Консоль: `[i18n] Translated 50+ elements`

## Repository

https://github.com/maxfraieho/violin.pp.ua
